## Toggle on / off UIErrorsFrame
this one toggles on and off melee spam
```
/run local f = UIErrorsFrame if f:IsVisible() then f:Hide() else f:Show() end
```


## Hide UIErrorsFrame
this one hides melee spam
```
/script UIErrorsFrame:Hide()
```